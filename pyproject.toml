[project]
name = "extruct-web"
version = "0.1.0"
description = "my extruct web service"
requires-python = ">=3.11"

dependencies = [
    "fastapi==0.115.4",
    "uvicorn==0.52.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"